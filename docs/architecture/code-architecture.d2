direction: down

platform: "PROCESS AND PLATFORM BOUNDARY\nkrita/main.cc · Windows stub · KisIOS*.mm\nstartup, OS lifecycle, application resources"

application: "APPLICATION COORDINATION — kritaui\nKisApplication · KisPart · KisMainWindow\nKisDocument · KisCanvas2 · input and tool base"

canvas: "CANVAS VIEW BOUNDARY — kritacanvas\ncoordinates · projection frames · display color transforms"

extensions: "RUNTIME EXTENSION BOUNDARY\nkoplugin · plugin metadata · registries\nplugins/* · qmlmodules"

engines: "CONTENT ENGINES\nkritaimage: nodes · tiles · strokes · projection\nbrush/pigment/color · flake · resources"

persistence: "PERSISTENCE BOUNDARY\nkritaimpex · kritaimpexui · plugins/impex\nresources/storage · serialization/xml · painting/metadata · psd*"

foundation: "SHARED FOUNDATION\nQt · KDE Frameworks · external libraries\nglobal · command · widgets · multiarch"

platform -> application: starts
application -> extensions: activates registries
application -> canvas: configures and presents
application -> engines: document, input, and tools
canvas -> engines: maps image and vector state
extensions -> engines: contributes capabilities
application -> persistence: opens and saves
persistence -> engines: imports / exports
foundation -> application: runtime services {
  style.stroke-dash: 5
}
foundation -> canvas: runtime services {
  style.stroke-dash: 5
}
foundation -> extensions: runtime services {
  style.stroke-dash: 5
}
foundation -> engines: algorithms and platform services {
  style.stroke-dash: 5
}
foundation -> persistence: codecs and containers {
  style.stroke-dash: 5
}
